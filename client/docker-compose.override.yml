 version: '3.7'
 services:
   dashboard:
     ports:
       - 5000:80
     networks:
       - network
     build: 
       context: ./
     container_name: PCNportal
     hostname: PCNportal
 networks:
   network:
     driver: bridge
