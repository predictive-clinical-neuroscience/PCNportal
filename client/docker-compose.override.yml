 version: '3.7'
 services:
   dashboard:
    #  volumes:
    #    - C:\Users\piebar\Documents\GitHub\pcnonlinedev\client\.ssh:/root/.ssh
     ports:
       - 5000:80
     networks:
       - network
     build: 
       context: ./
     container_name: PCNportal
     hostname: PCNportal
 networks:
   network:
     driver: bridge
